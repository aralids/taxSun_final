import * as React from "react";
import Wedge from "./Wedge.js";

const Plot = ({ lns, rks, txSet, rawTxSet }) => {
	const [mainState, setMainState] = React.useState({
		fileName: "",
		collaps: false,
		eValue: Infinity,
		name: "root",
		rank: "root",
	});
	return <></>;
};

export default Plot;
